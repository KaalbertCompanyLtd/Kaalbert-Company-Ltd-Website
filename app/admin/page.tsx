/**
 * Placeholder content area for the admin shell (T1.5) — the real dashboard (stat cards,
 * recent enquiries, quick actions) is content-management-admin.md's job, later in Milestone 7.
 */
export default function AdminDashboardPage() {
  return (
    <div>
      <h1 className="font-display text-h2 text-primary font-bold">Admin</h1>
      <p className="text-body text-muted-foreground mt-2">
        The admin shell frame is in place. Real dashboard content is built in a later milestone.
      </p>
    </div>
  );
}
